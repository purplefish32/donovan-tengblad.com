import decorateSelector from './decorateSelector';

var makeThemeColor = function makeThemeColor(key) {
  return decorateSelector(function (props) {
    return props.theme[key];
  });
};

export default makeThemeColor;